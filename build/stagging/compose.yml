version: '3.9'

networks:
  proxy:
    external: true
  internal:
    name: "zerotwo_staging_internal"

services:
  web:
    container_name: 02_django_stagging
    restart: unless-stopped
    build:
      context: ../../django
      dockerfile: ./compose/prod/Dockerfile
    networks:
      - internal
      - proxy
    env_file:
      - ./conf/django.env
      - ./conf/sql.env
    expose:
      - 8000
    depends_on:
      db:
        condition: service_healthy
      mqtt:
        condition: service_started
    labels:
      - traefik.enable=true
      - traefik.http.routers.django_02_staging.rule=Host(`staging.zerotwo.tenere.konar.pwr.edu.pl`)
      - traefik.http.routers.django_02_staging.entrypoints=websecure
      - traefik.http.routers.django_02_staging.tls.certresolver=leresolver
      - traefik.http.services.django_02_staging.loadbalancer.server.port=8000

  db:
    image: postgres
    container_name: 02_sql_stagging
    restart: always
    healthcheck:
          test: ["CMD", "pg_isready", "-U", "prod", "-d", "domena_db"]
          interval: 5s
          timeout: 10s
          retries: 120
    networks:
      - internal
    volumes:
      - ./sql/data/:/var/lib/postgresql/data
    env_file:
      - ./conf/sql.env
    expose:
      - 5432

  mqtt:
    image: eclipse-mosquitto:2
    container_name: 02_mqtt_stagging
    restart: always
    networks:
      - proxy
      - internal
    volumes:
      - type: bind 
        source: ./conf/mqtt/config/
        target: /mosquitto/config/
        read_only: true
      - type: bind
        source: ./conf/mqtt/log/
        target: /mosquitto/log/
        read_only: true
    expose:
      - 9001
    labels:
      - traefik.enable=true
      - traefik.http.routers.mqtt_02_staging.rule=Host(`staging.zerotwo.tenere.konar.pwr.edu.pl`) && PathPrefix(`/broker`)
      - traefik.http.routers.mqtt_02_staging.entrypoints=websecure
      - traefik.http.routers.mqtt_02_staging.tls.certresolver=leresolver
      - traefik.http.services.mqtt_02_staging.loadbalancer.server.port=9001
      - traefik.http.services.mqtt_02_staging.loadBalancer.sticky.cookie=true